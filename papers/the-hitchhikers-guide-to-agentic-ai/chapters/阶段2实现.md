            # 阶段2：实现
            prompt = (f"设计：\n{state.architecture}\n\n"
                      + (f"先前反馈：\n{state.review_feedback}\n\n"
                         if state.review_feedback else "")
                      + "编写实现代码。")
            state.code = await self.coder.invoke(prompt)

